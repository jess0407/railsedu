<h1><%= @soup.name%></h1>
<% if @soup.frature? %>
  <p>This delicious soup is featured</p>
<% else %>
  <p>
    Not in season, so not so good.
  </p>
<% end %>

<p>
  <%= link_to "Toggle Featured", toggle_featured_path(@soup)%>
</p>
